“摄像头已经能看到画面，但视觉引导缺失（没有扫描框和激光线），这导致对焦极其困难。

请执行以下 UI 修复：

强制叠加视觉层：在扫码容器（reader div）的父级创建一个 relative 定位的容器。

绘制扫描框：使用 CSS 在容器正中心绘制一个 280x180 的边框，四个角使用 L 型线条加粗（类似相机准心）。

添加激光扫描线：

在扫描框内添加一个 absolute 定位的横线。

使用 Tailwind 或 CSS 原生动画（Keyframes），让该线条在框内上下循环移动。

设置 pointer-events-none 确保该图层不影响点击操作。

提高层级：确保视觉引导层的 z-index 高于 html5-qrcode 生成的 video 标签。

对齐识别区域：确保视觉上的框，与代码中配置的 qrbox 参数在位置和尺寸上完全一致，确保“所见即所扫”。